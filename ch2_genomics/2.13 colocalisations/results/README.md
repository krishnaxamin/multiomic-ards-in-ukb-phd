# Data present
- `coloc_results*`: Collated results files produced from `abf_susie_coloc.R` and collated by `collate_coloc_results.py`.
- `coloc_summaries*`: Collated summary files produced from `abf_susie_coloc.R` and collated by `collate_coloc_results.py`.

# Data absent
- `abf/` and `susie/`: Results from individual runs produced from `abf_susie_coloc.R`. An individual run is a combination of a single disease and a single genomic region, listed in `../combo_regions_lifestyles.csv`.
